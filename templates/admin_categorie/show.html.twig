{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
        </tbody>
    </table>


    <button class="btn btn-warning"><a href="{{ path('app_admin_categorie_index') }}">Retour a la liste</a></button>

    <button class="btn btn-warning"><a href="{{  path('app_admin_categorie_edit', {'id': categorie.id}) }}">Modifier</a></button>


    {{ include('admin_categorie/_delete_form.html.twig') }}
{% endblock %}
