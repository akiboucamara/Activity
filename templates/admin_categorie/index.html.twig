{% extends 'base.html.twig' %}

{% block title %}Categorie index{% endblock %}

{% block body %}
    <h1>Categorie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.id }}</td>
                <td>{{ categorie.nom }}</td>
                <td>

                    <button class="btn btn-warning"><a href="{{ path('app_admin_categorie_show', {'id': categorie.id}) }}">Afficher</a></button>
                    <button class="btn btn-warning"><a href="{{ path('app_admin_categorie_edit', {'id': categorie.id}) }}">Modifier</a></button>
                    
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <button class="btn btn-warning"><a href="{{ path('app_admin_categorie_new') }}">Crée nouveaux</a></button>
    
{% endblock %}
