{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{ article.image }}" height="100" width="100"alt"..."></td>
            </tr>
        </tbody>
    </table>

    
    <button class="btn btn-danger"><a href="{{path('app_admin_article_index')}}">Retour en Arrière</a></button>

    <a href="{{ path('app_admin_article_edit', {'id': article.id}) }}">Modifier</a>

    {{ include('admin_article/_delete_form.html.twig') }}
{% endblock %}
